#include <iostream>
#include <cstring>
#include <string>
#include <cstdlib>
using namespace std;

int main() {
    int grades[5];
    int sum = 0;
    
    for (int i = 0; i < 5; i++) {
        cout << "Enter grade " << i + 1 << ": ";
        cin >> grades[i];
        sum += grades[i];
    }
        cout << "Sum = " << sum << endl;
}

