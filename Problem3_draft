#include <iostream>
using namespace std;

int main()
{
    float cup, total_price, total_discount, how_much;
    
    cout << "How many cups do you want?";
    cin >> cup;
    
    if (cup >= 4) {
        cout << "Please enjoy your lemonade. It's unfortunate you can't get a discount.";
    }
        
    if else (5 < cup > 9){ 
        total_price = cup * 1;
        total_discount = total_price / 10;
        total_price - total_discount = how_much;
        cout << "You can get " << cup << "cups for " << how_much;
    }
        
    else (cup <= 10) {
        total_price = cup * 1;
        total_discount = total price / 20;
        total_price - total_discount = how_much;
        cout << "You can get " << cup << "cups for " << how_much;
    } 

    return 0;
}
